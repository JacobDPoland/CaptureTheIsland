@{
    ViewData["Title"] = "Easy — Apache Log Basics";
}

<div class="container mt-4">

    <!-- Card wrapper -->
    <div class="card bg-secondary bg-opacity-25 border-light shadow-lg">
        <div class="card-body text-light">

            <h2 class="text-center mb-3" style="color:white;">🟩 Easy — Apache Log Basics</h2>

            <p>
                Analyze the Apache access log entries below and answer the questions.
                This challenge helps you practice reading timestamps, IP addresses,
                request methods, and status codes.
            </p>

<pre class="bg-dark p-3 border rounded">
192.168.1.10 - - [24/Nov/2025:13:10:23 -0600] "GET /index.html HTTP/1.1" 200 1043
10.0.0.5     - - [24/Nov/2025:13:12:48 -0600] "POST /login.php HTTP/1.1" 401 560
172.16.0.7   - - [24/Nov/2025:13:14:01 -0600] "GET /admin/panel.php HTTP/1.1" 403 702
192.168.1.10 - - [24/Nov/2025:13:15:55 -0600] "GET /images/banner.png HTTP/1.1" 200 982
</pre>

            <form asp-action="SubmitLogEasy" method="post" class="mt-3">

                <!-- Q1 -->
                <label class="mt-3"><strong>1. Which IP address attempted to access the restricted <code>/admin/panel.php</code> page?</strong></label>
                <select name="q1" class="form-select bg-dark text-light border-secondary">
                    <option value="">-- Select an answer --</option>
                    <option>192.168.1.10</option>
                    <option>172.16.0.7</option>
                    <option>10.0.0.5</option>
                </select>

                <!-- Q2 -->
                <label class="mt-3"><strong>2. What HTTP status code indicates the failed login attempt?</strong></label>
                <select name="q2" class="form-select bg-dark text-light border-secondary">
                    <option value="">-- Select an answer --</option>
                    <option>200</option>
                    <option>401</option>
                    <option>403</option>
                </select>

                <!-- Q3 -->
                <label class="mt-3">
                    <strong>3. What file was successfully accessed by <code>192.168.1.10</code> at 13:15:55?</strong>
                </label>
                <input name="q3" placeholder="Example: /something/file.html" class="form-control bg-dark text-light border-secondary" />

                <!-- Q4 -->
                <label class="mt-3"><strong>4. What HTTP method was used to attempt the login?</strong></label>
                <input name="q4" placeholder="Example: GET, POST, PUT..." class="form-control bg-dark text-light border-secondary" />

                <button type="submit" class="btn btn-primary mt-3">Submit Answers</button>
            </form>

            <!-- Result messages -->
            @if (TempData["Success"] != null)
            {
                <div class="alert alert-success mt-3">@TempData["Success"]</div>
            }

            @if (TempData["Error"] != null)
            {
                <div class="alert alert-danger mt-3">@TempData["Error"]</div>
            }

            <!-- Back & Next -->
            <div class="mt-4 d-flex justify-content-between">
                <a href="/Challenges/LogAnalysis" class="btn btn-outline-light">
                    ← Back to Log Analysis
                </a>
                <a href="/Challenges/LogMedium" class="btn btn-outline-light">
                    Next Challenge → Medium (SSH Brute-force)
                </a>
            </div>





            <!-- Hints -->
            <div class="accordion mt-4" id="hintsAccordion">
                <div class="accordion-item bg-dark text-light">
                    <h2 class="accordion-header">
                        <button class="accordion-button bg-dark text-light" type="button" data-bs-toggle="collapse" data-bs-target="#hint1">
                            Need a hint?
                        </button>
                    </h2>
                    <div id="hint1" class="accordion-collapse collapse">
                        <div class="accordion-body">
                            • The status code for failed login attempts is usually 401.
                            • Look for timestamps to match which file was accessed.
                            • Methods appear inside the quotes, such as <code>"POST /login.php"</code>.
                        </div>
                    </div>
                </div>
            </div>

            <!-- Full Walkthrough -->
            <div class="accordion mt-3" id="walkAccordion">
                <div class="accordion-item bg-dark text-light">
                    <h2 class="accordion-header">
                        <button class="accordion-button bg-dark text-light collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#walk">
                            Stuck? View Full Walk-Through
                        </button>
                    </h2>
                    <div id="walk" class="accordion-collapse collapse">
                        <div class="accordion-body">
                            <strong>Line-by-line guidance:</strong><br /><br />

                            1. The <code>/admin/panel.php</code> request is on the 3rd line → IP is <code>172.16.0.7</code>.<br />
                            2. Failed login attempts use the 401 HTTP code → appears on the 2nd line.<br />
                            3. At 13:15:55, the request is <code>/images/banner.png</code> from 192.168.1.10.<br />
                            4. The login attempt uses the HTTP method <code>POST</code>.

                            <hr />

                            <strong>Correct Answers:</strong><br />
                            • Q1 → 172.16.0.7<br />
                            • Q2 → 401<br />
                            • Q3 → /images/banner.png<br />
                            • Q4 → POST<br />
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>

</div>
